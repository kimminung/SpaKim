//
//  ContentView.swift
//  TestProject
//
//  Created by 민웅킴 on 2/28/24.
//

import SwiftUI

struct ContentView: View {
//    let literalColor : UIColor = #colorLiteral(
    var body: some View {
        VStack {
            Image(systemName: "globe")
                .imageScale(.large)
                .foregroundStyle(.tint)
            Text("MinKisdasda, world!")
        }
        .padding()
    }
}

#Preview {
    ContentView()
}
